# circuit-agents 使用说明书

> 把自然语言任务「编译成电路拓扑」来执行的 agent 编排系统。
> 本说明书面向**使用者**（跑任务、看输出、协同、运维），不面向开发者。
> 命令行/接口细节见 `README.md`、`SPEC.md`、`COMPILER.md`、`CIRCUIT_EXECUTOR_DESIGN.md`。

---

## 0. 这是什么

circuit-agents 把一次多步任务想象成一张**电路拓扑**：

| 电路元件 | 在任务里的含义 |
|---------|--------------|
| 电阻 resistor | 一个原子执行步骤（真正干活的节点） |
| 电容 capacitor | 缓冲 / 暂存中间结果 |
| 运放 opamp | 调度 / 汇合（把多条支路合并） |
| adc / verify | 质量门（打分、判断是否达标） |
| 开关 switch | 受控通断：隔离故障支路 / 启用备用支路 |
| 导线 wire | 数据 / 信号流向 |
| 反馈环 feedback | 重试 / 补数闭环 |

**元件图纸式编号（像真电路图一样识别每个元件）**：真实电路里同一类型的元件不止一个时，用「元件字母 + 序号」区分（灯泡 L1/L2、电阻 R1/R2、开关 S1/S2……）。circuit-agents 同样给每个元件分配图纸编号 `ref`——电阻 → `R1/R2/R3…`、电容 → `C1/C2…`、运放 → `U1…`、二极管 → `D1…`、质量门 → `ADC1…`、电源 → `P1…`、开关 → `S1/S2…`、看门狗 → `W1…`。同类型多实例在拓扑里各自独立、按序编号，电路图 / 实时时间线 / 任务输出面板都用编号寻址（如「`R3` 完成 ✓」）。例如一个"多源并行分析"任务会自动长成 `P1 → U1 → R1…R5 五电阻并联 → C1/C2/C3 多级汇合 → D1 → ADC1` 的真电路形态，同一个任务里出现多个电阻、多个电容是常态而非例外。

**开关元件（switch · S1/S2，像真电路的开关一样重要）**：开关控制一条支路的通断，`state=on` 合闸透传、`state=off` 跳闸开路（零成本、不调后端）。它有三级用法：
- **手动全切**：在 spec 里直接预设 `state`——主支路坏就扳 `S1: off`、备用支路 `S2: on`，坏处理器从源头隔离；
- **半自动**：手动隔离主支路（`S1: off`）后，反馈循环会自动把健康的备用开关合闸（`S2 → on`）；
- **全自动跳闸换路**（最常用的价值）：质量门反复不达标时，系统像电工一样自动**把输入已失败的合闸开关扳断（隔离故障支路）、把上游已恢复的跳闸开关合闸（接通备用支路）**，信号改道从健康支路继续——电路不会因为某一条支路坏掉而整体失败。每次换路记录在结果 `switch_flips`（如 `[('s1','off'), ('s2','on')]`），电路图上开关符号会画出 ON（接通）/ OFF（断开）状态。演示见 `examples/switch_isolation.json`。

你只需要用**自然语言描述目标**，系统自动编译成拓扑、执行，并把每一步的进展**实时**推给你看。

**熔断器元件（fuse · FU1，一次性保护，防反复烧钱）**：真电路里熔断器烧断就永久断开、需要换新，与可复位的开关互补。agent 语义正好对治一个真痛点——某条支路的处理器反复失败时，光靠开关换路会一轮轮重试、每次重试都烧 token。电路在**反馈预算耗尽仍失败**时会把该支路入口的保险丝**一次性烧断**：
- 本任务立即隔离该支路；烧断状态由看门狗粘滞持久化（`mark_blown`）
- **后续同类任务开局即 blown**——被保护的处理器拿不到输入，真 LLM 后端直接短路**零调用**，不再给注定失败的处理器烧钱
- 与开关的区别：自动换路机制（`_flip_switches`）**绝不碰保险丝**；确认处理器修复后由人工把 fuse 的 `state` 重置回 `ok`（换新保险丝）
- 每次熔断记入结果 `fuse_blown`（如 `['fuse1']`）；电路图上保险丝画成矩形壳+内部熔丝，烧断显示断开。演示见 `examples/fuse_blow.json`

它围绕三根北极星支柱设计，使用时能直接感受到：

1. **实时交互** —— 执行过程连续不中断，每步可见、可中途介入。
2. **人机协同** —— 机器不只在「卡住才问人」，而是把人类/其他 agent/知识源/外部系统都当**一等协作者**，主动招募最合适的来帮忙。
3. **大规模协作** —— 多人在同一张拓扑上并发操作也**无冲突**（底层用 CRDT，永不 409）。

---

## 0.1 小白 3 步上手（不想看长文版）

你只需要会「打字」和「点按钮」，不用懂任何技术名词：

1. **双击打开**：双击桌面上的「电路拓扑工作台」图标（或 `dist/circuit-agents.exe`），自动弹出一个窗口。
2. **说人话**：在「✦ 想让我帮你做什么？」框里，用你平时说话的方式写一句目标，例如 `把这组数字 5,3,9,1,7 加起来再排序`。
3. **点「开始」**：右边实时显示进度和每一步的产出，最后给你一份结果摘要。

> 窗口右上角有「模式: 简易 / 完整」按钮：**默认就是「简易」**，把专家才用的旋钮先藏起来；想看全部功能点一下切到「完整」即可。功能本身没变，只是把复杂的东西收起来，不吓人。

---

## 1. 快速开始

### 1.1 环境要求
- Python 3.13（项目自带隔离虚拟环境 `binaries/python/envs/default`）。
- Windows 桌面壳需要 WebView2 运行时（Win10/11 自带）。
- 默认监听 `127.0.0.1:8765`。

### 1.2 启动服务端（二选一）

**方式 A：直接跑源码**
```bash
# 在项目根目录
python server.py            # 默认 127.0.0.1:8765
python server.py --port 9000   # 自定义端口
python server.py --selftest    # 只跑离线自检（S1–S46），不启动服务
```

**方式 B：桌面双击（已打包，推荐小白）**
- 双击 `dist/circuit-agents.exe`（或桌面快捷方式「电路拓扑工作台」）。
- 程序在后台起本地服务，**自动弹出 Live Console（最简入口）窗口**；关窗即停服务。
- 无需浏览器标签、无黑框控制台。想用完整编辑器再点窗口里的链接或访问 `/topology/editor`。

### 1.3 打开界面
服务起来后，浏览器访问：

| 地址 | 界面 | 用途 |
|------|------|------|
| `http://127.0.0.1:8765/` | **Live Console** | 提交任务、看实时时间线 + **任务输出面板** + 结果栏 |
| `http://127.0.0.1:8765/topology/editor` | **拓扑编辑器** | 可视化编排、房间、协作者池、求援箱、实时着色 |

> ⚠️ 注意：**Live Console 在根路由 `/`**（桌面 exe 默认就开这个，最常用）；完整**拓扑编辑器在 `/topology/editor`**（功能最多）。
> 一般跑任务、看实时输出，用根地址 `/` 就够了；要可视化编排 / 房间 / 协作者才进编辑器。

健康检查：`GET http://127.0.0.1:8765/health` → `{"status":"ok",...}`

---

## 2. 三大界面

### 2.1 Live Console（根路由 `/`）—— 最常用的入口
左侧「新任务」输入框写好目标，点 **运行**：
- **质量门阈值**：adc/verify 节点的达标线（0~1，默认 0.8，越低越易通过）。
- **附件路径**：图片/语音文件路径，逗号分隔（按扩展名自动识别图/音）。
- 提交后右侧出现三块：
  1. **拓扑图**：节点随执行实时着色（绿✓完成 / 红✗失败 / 橙虚框补数 / 紫⚠进化）。
  2. **📤 任务输出面板**（时间线下）：按节点完成顺序列出每个节点的**真实产出**，
     递归把标量 / Signal / 聚合信号渲染成可读文本，对最终汇流节点标「最终输出」；右上「复制」可一键复制整块产出。
  3. **结果栏**：最终品质、总成本、节点数、总延迟、质量门、状态（成功 / 质量门未通过 / 部分节点失败）。
     - **「真推理」可见行**：结果栏底部多一行——真调 DeepSeek 时显示 **🧠 本次由 DeepSeek 生成 · 耗时 X.Xs · 消耗 Y token**（并列出本次各模型调用次数，如 `deepseek-chat×2 · deepseek-reasoner×1`）；走模拟器时显示「本次为模拟演示，未调用真实大模型」。让小白一眼确认"真的是大模型在干活"。
     - **「投入产出比」读数**（核心评估标尺）：真推理时再算一行 **📊 投入产出比 ≈ 每 1.0 质量分耗 N token（投入 T token + 耗时 Xs · 产出 质量 Q）· 高性价比/中等/投入偏大**。比值 = 投入÷产出、越小越好——把"少投入换多产出"这把标尺每次任务都摆到台面上，让优化有迹可循。模拟器/产出为 0 时给出对应提示而非乱算。
- 底部「⏱ 历史」列出过往任务，点任一条可**回放**其时间线与输出。
- 左下方「⚡ 批量执行」：每行一个任务，并发执行并统一汇聚（显示加速比、聚合质量）。
- 任务执行通过 **SSE 流**实时推送；断线自动回退轮询，不丢事件。
- 标题栏「🧠 当前模型」标识：实时显示当前后端——绿框 **DeepSeek · deepseek-chat** 表示真在调大模型思考；显示 **🧪 演示模式（模拟器）** 说明未检测到 `DEEPSEEK_API_KEY`、走确定性模拟。任务执行时该标识会轻微闪烁，表示"正在思考"。
- **推理档自动升档**：接了 DeepSeek 后，任务里**需要推理/分析/判断/对比/综合**的电阻节点（如"分析财报差异""对比方案优劣"）会自动切到 **`deepseek-reasoner` 推理模型**（更高准确率、会先长链思考），检索/格式化等轻节点仍走 `deepseek-chat`。无需任何配置，更难的任务自动用推理模型、质量更高。可用环境变量 `CA_LLM_REASONER_MODEL` 覆盖推理模型名（默认 `deepseek-reasoner`）。

### 2.2 拓扑编辑器（`/topology/editor`）
功能最全的可视化工作台：
- 实时时间线（连续不中断）、节点脉冲高亮当前执行节点。
- **房间 / 角色权限**：创建房间、邀请他人以 observer/student/reviewer/mentor/owner 身份加入。
- **协作者池**：管理人类/agent/知识源/系统类协作者，机器据此主动招募。
- **🆘 待办求援箱**：机器卡住主动点名求助时实时可见，可异步作答回灌。
- **改流向模式**：从节点拖到另一节点重定向输出（owner/mentor 可改）。
- **共享记忆与知识库**、**多 agent 编排**、**跨房间联邦**等都在这里操作。

### 2.3 房间与多人在线（Rooms）
把一张拓扑变成「多人协作空间」：
- 创建房间 → 拿到 `room_id` → 分享邀请链接。
- 成员带角色加入；房间模式下的操作会记 activity 并广播给在线成员。
- 实时 presence（在线/焦点/离线超时）、CRDT 并发编辑（任意人改同一节点都不冲突）。

---

## 3. 三大能力（对应北极星）

### 3.1 实时交互：连续不中断
- 执行从第一层就开始可见，每完成一个节点立刻推送到时间线/拓扑着色。
- **运行中可介入**：通过编辑器设质量门阈值（`set_gate`）、编辑拓扑（插入/替换/追加并行节点），
  改动在下一个检查点生效，**主进度不卡死**。
- 底层是事件总线（`_RealtimeHub`）+ SSE，慢消费者不会阻塞广播（有界队列 drop-oldest）。

### 3.2 人机协同：机器主动招募
- 协作者不分「人 / 机器」——人类、agent、知识源、外部系统都在**同一个统一池**里。
- 节点缺数据 / 声明 needs 未达标 / 质量低于阈值时，机器**主动求援**，按能力择优派单；
  等超时则**自动升级**下候选；没人能解就诚实报 `unanswered`，不吊死。
- 求助会出现在「🆘 待办求援箱」，任何人（或外部系统，如 MES）可异步作答，答案**回灌**该节点输出，不重跑整图。

### 3.3 大规模协作：CRDT 无冲突
- 房间默认 `concurrency="crdt"`：用 OR-Set（add-wins）+ LWW-Map（按 `(lamport, actor)` 全序）+ Lamport 时钟。
- op 自包含、幂等、可交换 → 多人乱序 / 重投也收敛到同一状态，**永不 409、零重试、无全局串行点**。
- 适合「人数众多」的并发编辑与协同执行。

---

## 4. 典型工作流

### 4.1 跑一个任务并看实时输出
1. 浏览器开 `http://127.0.0.1:8765/`（Live Console）。
2. 在「新任务」写目标，例如：`读取整数列表[5,3,9,1,7]，求和后归一化，再排序输出`。
3. 点 **运行**。
4. 观察：拓扑图逐节点着色 → 时间线滚动 → **任务输出面板**逐节点显示产出 → 结果栏给出最终品质/成本。
5. 点「⏱ 历史」里这条任务可随时回放。

### 4.2 运行中介入
1. 开 `http://127.0.0.1:8765/topology/editor`。
2. 提交任务后，在执行途中用「改流向模式」拖拽重定向，或在节点上设更严的质量门。
3. 改动在下一个检查点生效，主进度自然继续（不重启、不卡死）。

### 4.3 多人协同一个房间
1. 编辑器里「创建房间」拿到 `room_id`，复制邀请链接发给协作者。
2. 协作者以对应角色加入；房间模式下的编辑/操作实时广播，presence 显示谁在线、谁在看哪个节点。
3. 多人同时改拓扑也不冲突（CRDT）。

### 4.4 机器卡住时求援（待办求援箱）
1. 任务执行中若某节点缺数据/不达标，机器会自动在「🆘 待办求援箱」开一条求援。
2. 你（或外部系统）看到后，在求援箱里作答；答案回灌节点，任务继续。
3. 超时未答会自动升级给其他候选协作者；始终无人能解则诚实标记 `unanswered`。

---

## 5. API 速查表（常用）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/run` | 提交自然语言任务，返回 `run_id` |
| GET | `/run/{run_id}` | 查任务状态/结果/拓扑 spec |
| GET | `/run/{run_id}/stream` | **SSE 流**：逐节点 `node_done` + 最终 `result`/`error` |
| GET | `/api/history` | 历史任务列表 |
| POST | `/batch` | 批量并发执行多个任务，返回汇聚结果 |
| GET | `/health` | 健康检查 |
| POST | `/topology/session` | 开一个拓扑会话（编辑器/实时总线） |
| GET | `/topology/editor` | 拓扑编辑器页面 |
| GET | `/topology/stream/{sid}` | 编辑器实时事件 SSE |
| POST | `/topology/edit/{sid}` | 运行中编辑拓扑（set_gate / 插入 / 替换 / 改流向） |
| POST | `/rooms` | 创建房间 |
| POST | `/rooms/{rid}/join` · `/role` | 加入房间 / 设置角色 |
| GET | `/rooms/{rid}` · `/activity` · `/presence` · `/stats` | 房间信息 / 动态 / 在线 / 统计 |
| GET/POST | `/rooms/{rid}/crdt` · `/crdt/ops` | CRDT 状态 / 提交并发编辑 op |
| POST | `/collaborators` · GET `/collaborators?need=` | 注册 / 按能力择优查询协作者 |
| GET | `/help/pending` | 跨会话待办求援箱 |
| POST | `/help/{hid}/respond` | 应答求援（答案回灌节点） |
| POST | `/rooms/{rid}/orchestrate` | 房间内多角色编排（reviewer 仅 adjudicate） |
| POST | `/rooms/{rid}/memory/publish`·`/pull`·`/distill` | 共享记忆知识库：发布 / 拉取 / 蒸馏 |
| POST | `/simplify` | 奥卡姆剃刀化简（去冗余节点） |
| POST | `/quality/report` | 给定拓扑出质量报告 |
| POST | `/evolve` · `/evolve/suggest` | 多任务进化 / 进化建议 |
| POST | `/longtask` · `/pause` · `/resume` · `/wake` | 长周期任务（可暂停/恢复/心跳唤醒） |
| GET/POST | `/pi/heartbeat` · `/start` · `/stop` · `/tick` · `/roi` | π 永动心跳（开机自启；`/roi` 切换「按 π 贪 / 按 ROI 贪」） |
| POST | `/multirobot` · `/cluster` · `/decision` · `/rl/optimize` · `/federated/round` | 多机/集群/决策/强化/联邦等进阶能力 |

> 完整端点以 `server.py` 源码为准；本表列出使用者最常接触的 ones。

请求体（以 `/run` 为例）：
```json
{
  "goal": "你的自然语言任务",
  "route": true,
  "quality_threshold": 0.8,
  "memory_enabled": true,
  "evolve_enabled": true,
  "images": ["a.png"],
  "audio": ["b.wav"]
}
```

---

## 6. 配置与运维

- **端口**：默认 `8765`；`python server.py --port <端口>` 改。
- **持久化**：房间、记忆、CRDT op 落盘，重启自动重建活跃会话（深化③）。
- **π 永动心跳**：服务启动即自动开启（`/pi/heartbeat`），可通过 `start/stop/tick` 手动控制。
  - **升级：从「按 π 贪」到「按 ROI 贪」**。设环境变量 `CA_PI_ROI=1`（或 `/pi/heartbeat/start?roi=1`、运行时 `/pi/heartbeat/roi?enabled=1`）即切换为 ROI 导向：每拍用 `_roi_scores()` 给四个动作算「预期 产出/投入」（越大越贪），默认选最高分动作；π 数字仅作探索扰动以保持多样性。**每次 `/run` 后，系统自动把真实 token/质量/耗时回灌心跳**，所以评分器读到的是真实信号。系统连续良态（高质无失败累计到上限）时，心跳**自我节流**（idle），即「心跳自身 ROI 趋零则不贪」的纪律——不空转烧算力。`/pi/heartbeat` 现额外返回 `roi_guided / idle_pressure / throttle_count / last_roi_scores / last_roi_choice`。
- **并发限流（防 RPM 打满）**：层内并行与批量并行会把瞬时请求数放大成「批量路数 × 层内节点数」，真 LLM 链路撞 RPM 上限会被**整体锁接口冷却**——瓶颈在频率、不在 token 总量，余额充足也照样被锁。系统已内置进程内全局双闸门：
  - **并发闸门**：限制同时飞行的请求数，环境变量 `CA_MAX_CONCURRENCY`（默认 `16`）。设 `1` 即退化为串行——牺牲时间、保护频率额度（与并行换速度互为取舍）。
  - **RPM 令牌桶**：限制每分钟请求数，环境变量 `CA_RPM`（默认 `0` = 不限频率，仅并发闸门生效）。
  - **429 退避**：撞 429 / 5xx 自动重试，优先尊重服务端 `Retry-After`，缺失则指数退避（0.5s → 1s → 2s，封顶 8s，最多重试 3 次）；网络层错误（断网 / DNS）快速失败、不把掉线当限流空等。
  - 两个真后端（`RealLLMBackend` / `OllamaBackend`）共用同一份闸门与退避实现，不会因后端不同而产生两套限流语义。
  - **自适应限流（AIMD，默认开）**：上面的静态闸门是「固定宽度」，但真后端的 RPM 上限你往往不知道。自适应模式会**实时检测真后端反馈、自动把并发调到限制边上的最大值**——既保证不撞限、又做到限制下的最大并行：
    - 检测信号：`post_with_retry` 每次 HTTP 把 ① 429 带 `Retry-After`（真撞限）② 429 无 `Retry-After`（软撞限）③ 单次延迟 > 近期基线 ×1.5（领先指标，撞限前预警）④ 连续成功（有余量）回灌给 `RATE_LIMITER`。
    - 实时调整（与 TCP 拥塞控制同构）：撞限 → **乘性减半**并进入串行冷却（真 429 直接降到 1 路直到 `Retry-After` 过期）；连续成功攒够窗口 → **加性 +1** 一路上探（默认无硬上限，纯靠反馈自收敛）；延迟领先 → **温和 −1** 提前退避（预测式防撞限）。
    - 旋钮：`CA_ADAPTIVE`（默认 `1`，设 `0` 退回纯静态闸门）、`CA_ADAPTIVE_CEIL`（默认**无上限**=极大值，AIMD 撞限自动收敛、不设人为封顶；如需防失控可设具体值如 `16`）、`CA_MAX_CONCURRENCY`（现为**起始并发**，默认 `16`）、`CA_RPM`（自适应下默认 `0`=不限频，撞限时由 429 自动触发 RPM 令牌桶临时钳制作双保险）。
    - 因为并发数直接决定单位时间请求数（速率 ≈ 并发 ÷ 单请求耗时），只调并发 AIMD 就能同时压住 RPM；状态可在 `RATE_LIMITER.snapshot()` 观测（`max_concurrency`=当前自动并发、`ceil`、`throttle_count`、`latency_base`）。

- **离线自检**：`python server.py --selftest` 跑 S1–S46，全过即核心链路健康。
- **桌面壳**：`desktop_launcher.py` 用 PyWebView 套壳；`make_desktop_shortcut.py` 生成桌面 `.lnk` 快捷方式。
- **提交/推送**：项目约定 `git push origin main` 走 WARP 代理、失败重试；请用 `git ls-remote` 校验本地与远端一致。

---

## 7. 常见问题（FAQ）

**Q：为什么在 Live Console 看不到任务输出？**
A：两个常见原因——
1. 访问错了地址：桌面 exe **现在默认就开 Live Console（`/`）**；如果你手动打开的是 `/topology/editor`（编辑器），那页不显示任务输出面板。
   看任务输出请用根路由 **`http://127.0.0.1:8765/`**。
2. 旧版本 SSE 流会崩：节点产出里含 `Signal` 对象，裸 `json.dumps` 会抛错导致流提前关闭。
   已修复（`server.py` 的 `_json_default` 兜底），**请确保用的是含该修复的版本**（`commit 28a7e8a` 之后）。

**Q：为什么结果看起来像是模板 / 示例，不像真人思考出来的？**
A：默认后端是**演示模拟器（SimBackend）**——它能完整跑通「分解 → 调度 → 质量门 → 重试 → 协同求援」整套流程，但产出是结构化的示意结果，不是大模型自由生成。
   想要「像 AI agent 那样真正思考 / 生成」，需要接一个真实模型后端。最简单的一条路：**只要环境变量 `DEEPSEEK_API_KEY` 已设置**（本机已具备），系统会自动把"真正干活的电阻节点"切到 **DeepSeek 真实推理**（OpenAI 兼容接口，零代码改动），其余结构件仍走确定性逻辑；没设 key 时回退到 SimBackend 演示模拟器。也可接本地 Ollama（`OllamaBackend`）或在编辑器 `/models/select` 选择其他模型。详见第 6 节与 `README.md`。

**Q：任务执行中途能改吗？**
A：能。用拓扑编辑器设质量门、拖拽改流向、插入/替换节点，改动在下一个检查点生效，主进度不卡死。

**Q：多人同时编辑会冲突吗？**
A：不会。房间默认 CRDT 模式，乱序/重投都收敛，永不 409。

**Q：机器真的会自己找人帮忙？**
A：会。节点卡住（缺数据/不达标）时主动开求援，出现在「🆘 待办求援箱」，可异步作答回灌；超时自动升级；无人能解诚实上报。

**Q：批量任务和单任务有什么区别？**
A：批量（`/batch`）并发跑多个目标并汇聚，报告加速比与聚合质量；单任务走 Live Console 的「新任务」。

---

## 8. 进阶能力（简要）

- **批量并行**：`/batch` 多目标并发 + 统一汇聚。
- **长周期任务**：`/longtask` 可暂停/恢复、心跳唤醒，适合跨天任务。
- **进化**：`/evolve` 多任务自动进化拓扑；`/simplify` 奥卡姆剃刀去冗余。
- **共享记忆与知识库**：房间内发布/拉取/蒸馏经验，跨会话复用。
- **跨房间联邦**：`/federation` + 单向汇入，多个房间知识互通。
- **多机/集群/决策/强化/联邦**：`/multirobot` `/cluster` `/decision` `/rl/optimize` `/federated/round` 等面向更大规模编排。

---

## 附录 A：真电路元件 ↔ circuit-agents 元件语言对照

真电路元件种类很多（没有固定总数：按功能大类可分 7 类、按理想元件是另一套、初中物理又只取常用几个）。本表是 circuit-agents 的元件语言与真实电路的对照——**✓ 已实现为元件类型 / ○ 由现有机制承载 / · 纯概念**。需要新增元件类型时先查此表，避免重复造轮子。

| 功能大类 | 真实元件 | circuit-agents 对应 | 状态 |
|---|---|---|---|
| ① 电源 | 电池 / 发电机 / 信号源 | `power`（任务源）、`source`（外部数据源） | ✓ |
| ① 电源 | 蓄电池（可充电储能） | TopologyMemory / 模板复用（跨任务记忆） | ○ |
| ② 负载用电器 | 电阻 | `resistor`（原子执行步骤，系统核心） | ✓ |
| ② 负载用电器 | 灯泡 | Live Console「最终输出」展示位 | · |
| ② 负载用电器 | 电动机（持续运转） | 长周期任务 `/longtask` | ○ |
| ③ 控制与保护 | 开关 / 断路器 | `switch`（受控通断 + 自动跳闸换路，可复位） | ✓ |
| ③ 控制与保护 | 熔断器 | `fuse`（一次性熔断，跨任务持久隔离防烧钱） | ✓ |
| ③ 控制与保护 | 继电器（弱信号控强电路） | 低质量信号触发子电路/进化 | · |
| ③ 控制与保护 | adc/比较器 | `adc` / `verify`（质量门：达标放行/跳闸重试） | ✓ |
| ④ 导线与连接 | 导线 | `wires` | ✓ |
| ④ 导线与连接 | 接线柱 / 插头 | `component_io` / `input_map`（接口声明与对接） | ✓ |
| ⑤ 测量元件 | 电流表 / 电压表 / 万用表 | `watchdog`（质量采样、劣化识别）+ 结果栏遥测 | ✓ |
| ⑥ 储能 | 电容 | `capacitor`（汇合 / 缓冲 / 上下文总线） | ✓ |
| ⑥ 储能 | 电感（平滑电流、抑制突变） | `RateLimiter` 并发/RPM 平滑节流 | ○ |
| ⑦ 半导体 | 二极管 | `diode`（单向校验/整流） | ✓ |
| ⑦ 半导体 | 集成电路 IC | `templates` / 技能包（封装复用） | ○ |
| — 理想元件 | 电压源 / 电流源 | `power` / `source` | ✓ |
| — 初中物理 | 滑动变阻器（可调电阻） | `OnlineTuner` 在线调参 / adc 阈值可调 | ○ |

图纸编号字母表（`compiler/refs.py`）：power→P、source→SRC、resistor→R、capacitor→C、diode→D、opamp→U、adc→ADC、watchdog→W、fuse→FU、format_adapter→F、switch→S、lamp→L、logic_gate→G、verify→V、buzzer→BZ、transformer→T；未知类型自动取类型名前 2 个大写字母。

*说明书基于当前代码（`commit` 含 SSE 序列化修复与「📤 任务输出面板」）编写；如接口有变动以 `server.py` 与 `README.md` 为准。*
