# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local state / secrets
.watchdog_state.json
.env
.env.*
*.local

# Editor / OS
.DS_Store
*.swp
.idea/

# Topology memory cache (auto-generated)
.topology_memory.json

# ⑦ 长周期任务 checkpoint 文件（运行时生成，含断点续跑状态）
.longtask_*.json
longtask_*.json

# Throwaway demo artifacts
_demo_out.txt
_mk_pareto.py
_append_*.md

# ⑬ 电路图共享生态——实时发布仓库（运行时生成，含 sha256 校验）
.topology_repo.json

# 深化③ 房间持久化落盘（运行时生成，重启恢复用，非 VCS）
.rooms.json
.rooms.json.tmp

# Runtime execution DB (auto-generated by circuit executor)
executions.db

# Live run artifacts (contain model outputs, not for VCS)
_live_mentor_result.json
_live_*.json
_live_*.log
_ot_server.log
.bridge.log
.wb_write_test.txt
