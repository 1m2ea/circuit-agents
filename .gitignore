# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local state / secrets
.watchdog_state.json
.env
.env.*
*.local

# Editor / OS
.DS_Store
*.swp
.idea/

# Topology memory cache (auto-generated)
.topology_memory.json

# P0 不可写面 holdout 编译缓存（auto-generated；clear_cache() 可清）
.holdout_spec_cache.json

# ⑦ 长周期任务 checkpoint 文件（运行时生成，含断点续跑状态）
.longtask_*.json
longtask_*.json

# Throwaway demo artifacts
_demo_out.txt
_mk_pareto.py
_append_*.md

# ⑬ 电路图共享生态——实时发布仓库（运行时生成，含 sha256 校验）
.topology_repo.json

# 深化③ 房间持久化落盘（运行时生成，重启恢复用，非 VCS）
.rooms.json
.rooms.json.tmp

# Runtime execution DB (auto-generated by circuit executor)
executions.db

# Live run artifacts (contain model outputs, not for VCS)
_live_mentor_result.json
_live_*.json
_live_*.log
_ot_server.log
.bridge.log
.wb_write_test.txt

# Debug / throwaway probe artifacts (not for VCS)
_editor_served.html
_fedlive.log
_procs*.txt
_s*.log
_test_*.py
_ui_*.log
_holdout_real_probe.py

# PyInstaller 桌面壳构建产物（含 19MB 单文件 exe，本地使用、不入库）
dist/
build/
circuit-agents.spec

# 会话工作区数据 / 基准产物（可由 benchmark_surpass.py 重生成，2026-09-09 副本收敛批次）
.workbuddy/
benchmark_out*/

# 全局日志兜底 + 散落临时输出
*.log
_ds_o.txt
_dsh_gui_*.txt
_health_selftest.err
_subconscious_bak_*/

# MCP 引擎在仓库根产生的运行态文件（2026-09-11 收编后新增，由 circuit-harness 补）
.operator_policy.json
.topology_memory_pending.json
_mcp_topo.json

# 技能 master 的运行期产物（随每次规划生成，不纳入版本控制）
skills/circuit-planner/scripts/runbooks/
skills/circuit-planner/scripts/diagrams/
skills/circuit-planner/scripts/__pycache__/

# 9/12 复核批次
._health_selftest.*
skills/circuit-planner/scripts/outputs/
